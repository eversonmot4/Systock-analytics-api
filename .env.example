# Variáveis de ambiente de exemplo para a API SyStock Analytics
# Defina `DATABASE_URL` com a string de conexão do banco (Postgres ou MySQL)
# Exemplos:
# Postgres: postgresql://user:password@host:5432/dbname
# MySQL:    mysql+pymysql://user:password@host:3306/dbname

DATABASE_URL=

# Prefixo opcional para views caso elas estejam sob um schema/prefixo específico
# ex: "data_warehouse." ou "analytics.". Se vazio, as views serão referenciadas apenas pelo nome.
DB_VIEW_PREFIX=

# Configurações do Uvicorn
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000
